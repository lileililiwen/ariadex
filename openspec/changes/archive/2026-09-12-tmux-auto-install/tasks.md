## 1. Setup

- [x] 1.1 Implement tmux detection and package-manager probing.
- [x] 1.2 Implement unattended install with non-interactive sudo and typed failures.
- [x] 1.3 Wire ensure step into `run`/`auto` loop and `attach`, plus `--no-auto-install`.

## 2. Verification

- [x] 2.1 Add setup unit tests (mocked installs only) and CLI wiring tests.
- [x] 2.2 Run tests and strict OpenSpec validation; record results in `HANDOFF.md`.
