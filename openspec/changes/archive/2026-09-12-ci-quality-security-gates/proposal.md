## Why

There is no CI/CD workflow, formatter, linter, type check, coverage gate, dependency scan, artifact build, or strict OpenSpec gate.

## What Changes

- Add CI across supported Python versions.
- Run unit, live, packaging, OpenSpec, formatting, lint, type, coverage, and security checks.
- Add protected release workflow and artifact verification.

## Non-goals

- No automatic deployment of user workspaces.

