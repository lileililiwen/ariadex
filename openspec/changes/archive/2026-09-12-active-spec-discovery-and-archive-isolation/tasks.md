## Tasks

- [x] Define the active-change discovery predicate and reserved archive path.
- [x] Apply the predicate consistently to graph loading, repository inspection, doctor, preview, and resync.
- [x] Add regression tests proving archived changes are never scheduled.
- [x] Add an empty-after-archive CLI smoke test.
- [x] Run tests and `openspec validate --changes --strict --no-interactive`.
- [ ] Record evidence in `HANDOFF.md`.
