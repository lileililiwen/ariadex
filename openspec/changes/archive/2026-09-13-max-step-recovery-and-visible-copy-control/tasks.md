# Tasks

- [x] Add regression tests for max-step classification, recovery prompt
      delivery, and collapsed-widget Copy log visibility.
- [x] Classify conservative OpenCode max-step-limit markers as a recoverable
      boundary before generic provider errors.
- [x] Route the recoverable boundary through existing task-aware prompt
      selection and fresh-ready checks without weakening generic error safety.
- [x] Move the managed Copy log control into the always-visible collapsed
      control row while preserving expanded Copy context behavior.
- [x] Run focused tests, full tests, Ruff, mypy, coverage floors, strict
      OpenSpec validation, and git diff checks.
