# Design: OpenCode composer formatting

The OpenCode adapter trims each captured UI line and recognizes the provider
footer by its UI glyph and `Build` label, allowing variable spacing between
them. The blank composer glyph remains required. No assistant text is
examined. The existing watcher then invokes OpenSpec evidence unchanged.
