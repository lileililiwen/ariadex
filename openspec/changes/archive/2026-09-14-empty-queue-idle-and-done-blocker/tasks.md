# Tasks

- [x] Return idle for an empty active queue in `select_next_action` regardless of stale targets.
- [x] Leave `blocker` empty on the done shutdown diagnostic.
- [x] Add regression tests for both behaviors.
- [x] Run focused tests plus `openspec validate --changes --strict --no-interactive`.
- [x] Archive the completed change.
