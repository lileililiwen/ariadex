# Tasks

- [x] Define the `ariadex dev setup` command, result states, and safety flags.
- [x] Implement user-scoped `uv` detection and supported installation paths.
- [x] Implement locked dev-environment sync and post-install tool checks.
- [x] Commit `uv.lock` generated from the pinned `dev` extra.
- [x] Update CI and release workflows to use `setup-uv`, frozen sync, and
      `uv run` security/quality commands.
- [x] Add regression tests for bootstrap behavior and workflow wiring.
- [x] Update README, ROADMAP, HANDOFF, and AGENTS with the new workflow.
- [x] Run unit, OpenSpec, lint, format, type, coverage, and security checks.
